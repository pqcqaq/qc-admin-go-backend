// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"go-backend/database/ent/predicate"
	"go-backend/database/ent/workflowapplication"
	"go-backend/database/ent/workflowedge"
	"go-backend/database/ent/workflownode"
	"go-backend/database/ent/workflownodeexecution"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WorkflowNodeQuery is the builder for querying WorkflowNode entities.
type WorkflowNodeQuery struct {
	config
	ctx                    *QueryContext
	order                  []workflownode.OrderOption
	inters                 []Interceptor
	predicates             []predicate.WorkflowNode
	withApplication        *WorkflowApplicationQuery
	withExecutions         *WorkflowNodeExecutionQuery
	withOutgoingEdges      *WorkflowEdgeQuery
	withIncomingEdges      *WorkflowEdgeQuery
	modifiers              []func(*sql.Selector)
	withNamedExecutions    map[string]*WorkflowNodeExecutionQuery
	withNamedOutgoingEdges map[string]*WorkflowEdgeQuery
	withNamedIncomingEdges map[string]*WorkflowEdgeQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WorkflowNodeQuery builder.
func (_q *WorkflowNodeQuery) Where(ps ...predicate.WorkflowNode) *WorkflowNodeQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *WorkflowNodeQuery) Limit(limit int) *WorkflowNodeQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *WorkflowNodeQuery) Offset(offset int) *WorkflowNodeQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *WorkflowNodeQuery) Unique(unique bool) *WorkflowNodeQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *WorkflowNodeQuery) Order(o ...workflownode.OrderOption) *WorkflowNodeQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryApplication chains the current query on the "application" edge.
func (_q *WorkflowNodeQuery) QueryApplication() *WorkflowApplicationQuery {
	query := (&WorkflowApplicationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workflownode.Table, workflownode.FieldID, selector),
			sqlgraph.To(workflowapplication.Table, workflowapplication.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, workflownode.ApplicationTable, workflownode.ApplicationColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryExecutions chains the current query on the "executions" edge.
func (_q *WorkflowNodeQuery) QueryExecutions() *WorkflowNodeExecutionQuery {
	query := (&WorkflowNodeExecutionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workflownode.Table, workflownode.FieldID, selector),
			sqlgraph.To(workflownodeexecution.Table, workflownodeexecution.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, workflownode.ExecutionsTable, workflownode.ExecutionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOutgoingEdges chains the current query on the "outgoing_edges" edge.
func (_q *WorkflowNodeQuery) QueryOutgoingEdges() *WorkflowEdgeQuery {
	query := (&WorkflowEdgeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workflownode.Table, workflownode.FieldID, selector),
			sqlgraph.To(workflowedge.Table, workflowedge.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, workflownode.OutgoingEdgesTable, workflownode.OutgoingEdgesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryIncomingEdges chains the current query on the "incoming_edges" edge.
func (_q *WorkflowNodeQuery) QueryIncomingEdges() *WorkflowEdgeQuery {
	query := (&WorkflowEdgeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workflownode.Table, workflownode.FieldID, selector),
			sqlgraph.To(workflowedge.Table, workflowedge.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, workflownode.IncomingEdgesTable, workflownode.IncomingEdgesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first WorkflowNode entity from the query.
// Returns a *NotFoundError when no WorkflowNode was found.
func (_q *WorkflowNodeQuery) First(ctx context.Context) (*WorkflowNode, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{workflownode.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *WorkflowNodeQuery) FirstX(ctx context.Context) *WorkflowNode {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WorkflowNode ID from the query.
// Returns a *NotFoundError when no WorkflowNode ID was found.
func (_q *WorkflowNodeQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{workflownode.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *WorkflowNodeQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WorkflowNode entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WorkflowNode entity is found.
// Returns a *NotFoundError when no WorkflowNode entities are found.
func (_q *WorkflowNodeQuery) Only(ctx context.Context) (*WorkflowNode, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{workflownode.Label}
	default:
		return nil, &NotSingularError{workflownode.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *WorkflowNodeQuery) OnlyX(ctx context.Context) *WorkflowNode {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WorkflowNode ID in the query.
// Returns a *NotSingularError when more than one WorkflowNode ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *WorkflowNodeQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{workflownode.Label}
	default:
		err = &NotSingularError{workflownode.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *WorkflowNodeQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WorkflowNodes.
func (_q *WorkflowNodeQuery) All(ctx context.Context) ([]*WorkflowNode, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WorkflowNode, *WorkflowNodeQuery]()
	return withInterceptors[[]*WorkflowNode](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *WorkflowNodeQuery) AllX(ctx context.Context) []*WorkflowNode {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WorkflowNode IDs.
func (_q *WorkflowNodeQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(workflownode.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *WorkflowNodeQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *WorkflowNodeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*WorkflowNodeQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *WorkflowNodeQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *WorkflowNodeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *WorkflowNodeQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WorkflowNodeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *WorkflowNodeQuery) Clone() *WorkflowNodeQuery {
	if _q == nil {
		return nil
	}
	return &WorkflowNodeQuery{
		config:            _q.config,
		ctx:               _q.ctx.Clone(),
		order:             append([]workflownode.OrderOption{}, _q.order...),
		inters:            append([]Interceptor{}, _q.inters...),
		predicates:        append([]predicate.WorkflowNode{}, _q.predicates...),
		withApplication:   _q.withApplication.Clone(),
		withExecutions:    _q.withExecutions.Clone(),
		withOutgoingEdges: _q.withOutgoingEdges.Clone(),
		withIncomingEdges: _q.withIncomingEdges.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithApplication tells the query-builder to eager-load the nodes that are connected to
// the "application" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkflowNodeQuery) WithApplication(opts ...func(*WorkflowApplicationQuery)) *WorkflowNodeQuery {
	query := (&WorkflowApplicationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withApplication = query
	return _q
}

// WithExecutions tells the query-builder to eager-load the nodes that are connected to
// the "executions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkflowNodeQuery) WithExecutions(opts ...func(*WorkflowNodeExecutionQuery)) *WorkflowNodeQuery {
	query := (&WorkflowNodeExecutionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withExecutions = query
	return _q
}

// WithOutgoingEdges tells the query-builder to eager-load the nodes that are connected to
// the "outgoing_edges" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkflowNodeQuery) WithOutgoingEdges(opts ...func(*WorkflowEdgeQuery)) *WorkflowNodeQuery {
	query := (&WorkflowEdgeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOutgoingEdges = query
	return _q
}

// WithIncomingEdges tells the query-builder to eager-load the nodes that are connected to
// the "incoming_edges" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkflowNodeQuery) WithIncomingEdges(opts ...func(*WorkflowEdgeQuery)) *WorkflowNodeQuery {
	query := (&WorkflowEdgeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withIncomingEdges = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WorkflowNode.Query().
//		GroupBy(workflownode.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *WorkflowNodeQuery) GroupBy(field string, fields ...string) *WorkflowNodeGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WorkflowNodeGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = workflownode.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.WorkflowNode.Query().
//		Select(workflownode.FieldCreateTime).
//		Scan(ctx, &v)
func (_q *WorkflowNodeQuery) Select(fields ...string) *WorkflowNodeSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &WorkflowNodeSelect{WorkflowNodeQuery: _q}
	sbuild.label = workflownode.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WorkflowNodeSelect configured with the given aggregations.
func (_q *WorkflowNodeQuery) Aggregate(fns ...AggregateFunc) *WorkflowNodeSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *WorkflowNodeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !workflownode.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *WorkflowNodeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WorkflowNode, error) {
	var (
		nodes       = []*WorkflowNode{}
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withApplication != nil,
			_q.withExecutions != nil,
			_q.withOutgoingEdges != nil,
			_q.withIncomingEdges != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WorkflowNode).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WorkflowNode{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withApplication; query != nil {
		if err := _q.loadApplication(ctx, query, nodes, nil,
			func(n *WorkflowNode, e *WorkflowApplication) { n.Edges.Application = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withExecutions; query != nil {
		if err := _q.loadExecutions(ctx, query, nodes,
			func(n *WorkflowNode) { n.Edges.Executions = []*WorkflowNodeExecution{} },
			func(n *WorkflowNode, e *WorkflowNodeExecution) { n.Edges.Executions = append(n.Edges.Executions, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withOutgoingEdges; query != nil {
		if err := _q.loadOutgoingEdges(ctx, query, nodes,
			func(n *WorkflowNode) { n.Edges.OutgoingEdges = []*WorkflowEdge{} },
			func(n *WorkflowNode, e *WorkflowEdge) { n.Edges.OutgoingEdges = append(n.Edges.OutgoingEdges, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withIncomingEdges; query != nil {
		if err := _q.loadIncomingEdges(ctx, query, nodes,
			func(n *WorkflowNode) { n.Edges.IncomingEdges = []*WorkflowEdge{} },
			func(n *WorkflowNode, e *WorkflowEdge) { n.Edges.IncomingEdges = append(n.Edges.IncomingEdges, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedExecutions {
		if err := _q.loadExecutions(ctx, query, nodes,
			func(n *WorkflowNode) { n.appendNamedExecutions(name) },
			func(n *WorkflowNode, e *WorkflowNodeExecution) { n.appendNamedExecutions(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedOutgoingEdges {
		if err := _q.loadOutgoingEdges(ctx, query, nodes,
			func(n *WorkflowNode) { n.appendNamedOutgoingEdges(name) },
			func(n *WorkflowNode, e *WorkflowEdge) { n.appendNamedOutgoingEdges(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedIncomingEdges {
		if err := _q.loadIncomingEdges(ctx, query, nodes,
			func(n *WorkflowNode) { n.appendNamedIncomingEdges(name) },
			func(n *WorkflowNode, e *WorkflowEdge) { n.appendNamedIncomingEdges(name, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *WorkflowNodeQuery) loadApplication(ctx context.Context, query *WorkflowApplicationQuery, nodes []*WorkflowNode, init func(*WorkflowNode), assign func(*WorkflowNode, *WorkflowApplication)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*WorkflowNode)
	for i := range nodes {
		fk := nodes[i].ApplicationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(workflowapplication.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "application_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *WorkflowNodeQuery) loadExecutions(ctx context.Context, query *WorkflowNodeExecutionQuery, nodes []*WorkflowNode, init func(*WorkflowNode), assign func(*WorkflowNode, *WorkflowNodeExecution)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uint64]*WorkflowNode)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(workflownodeexecution.FieldNodeID)
	}
	query.Where(predicate.WorkflowNodeExecution(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(workflownode.ExecutionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.NodeID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "node_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *WorkflowNodeQuery) loadOutgoingEdges(ctx context.Context, query *WorkflowEdgeQuery, nodes []*WorkflowNode, init func(*WorkflowNode), assign func(*WorkflowNode, *WorkflowEdge)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uint64]*WorkflowNode)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(workflowedge.FieldSourceNodeID)
	}
	query.Where(predicate.WorkflowEdge(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(workflownode.OutgoingEdgesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SourceNodeID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "source_node_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *WorkflowNodeQuery) loadIncomingEdges(ctx context.Context, query *WorkflowEdgeQuery, nodes []*WorkflowNode, init func(*WorkflowNode), assign func(*WorkflowNode, *WorkflowEdge)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uint64]*WorkflowNode)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(workflowedge.FieldTargetNodeID)
	}
	query.Where(predicate.WorkflowEdge(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(workflownode.IncomingEdgesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.TargetNodeID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "target_node_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *WorkflowNodeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *WorkflowNodeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(workflownode.Table, workflownode.Columns, sqlgraph.NewFieldSpec(workflownode.FieldID, field.TypeUint64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, workflownode.FieldID)
		for i := range fields {
			if fields[i] != workflownode.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withApplication != nil {
			_spec.Node.AddColumnOnce(workflownode.FieldApplicationID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *WorkflowNodeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(workflownode.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = workflownode.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *WorkflowNodeQuery) ForUpdate(opts ...sql.LockOption) *WorkflowNodeQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *WorkflowNodeQuery) ForShare(opts ...sql.LockOption) *WorkflowNodeQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// WithNamedExecutions tells the query-builder to eager-load the nodes that are connected to the "executions"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkflowNodeQuery) WithNamedExecutions(name string, opts ...func(*WorkflowNodeExecutionQuery)) *WorkflowNodeQuery {
	query := (&WorkflowNodeExecutionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedExecutions == nil {
		_q.withNamedExecutions = make(map[string]*WorkflowNodeExecutionQuery)
	}
	_q.withNamedExecutions[name] = query
	return _q
}

// WithNamedOutgoingEdges tells the query-builder to eager-load the nodes that are connected to the "outgoing_edges"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkflowNodeQuery) WithNamedOutgoingEdges(name string, opts ...func(*WorkflowEdgeQuery)) *WorkflowNodeQuery {
	query := (&WorkflowEdgeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedOutgoingEdges == nil {
		_q.withNamedOutgoingEdges = make(map[string]*WorkflowEdgeQuery)
	}
	_q.withNamedOutgoingEdges[name] = query
	return _q
}

// WithNamedIncomingEdges tells the query-builder to eager-load the nodes that are connected to the "incoming_edges"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkflowNodeQuery) WithNamedIncomingEdges(name string, opts ...func(*WorkflowEdgeQuery)) *WorkflowNodeQuery {
	query := (&WorkflowEdgeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedIncomingEdges == nil {
		_q.withNamedIncomingEdges = make(map[string]*WorkflowEdgeQuery)
	}
	_q.withNamedIncomingEdges[name] = query
	return _q
}

// WorkflowNodeGroupBy is the group-by builder for WorkflowNode entities.
type WorkflowNodeGroupBy struct {
	selector
	build *WorkflowNodeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *WorkflowNodeGroupBy) Aggregate(fns ...AggregateFunc) *WorkflowNodeGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *WorkflowNodeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkflowNodeQuery, *WorkflowNodeGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *WorkflowNodeGroupBy) sqlScan(ctx context.Context, root *WorkflowNodeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WorkflowNodeSelect is the builder for selecting fields of WorkflowNode entities.
type WorkflowNodeSelect struct {
	*WorkflowNodeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *WorkflowNodeSelect) Aggregate(fns ...AggregateFunc) *WorkflowNodeSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *WorkflowNodeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkflowNodeQuery, *WorkflowNodeSelect](ctx, _s.WorkflowNodeQuery, _s, _s.inters, v)
}

func (_s *WorkflowNodeSelect) sqlScan(ctx context.Context, root *WorkflowNodeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
