// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"go-backend/database/ent/predicate"
	"go-backend/database/ent/workflowapplication"
	"go-backend/database/ent/workflowedge"
	"go-backend/database/ent/workflowexecution"
	"go-backend/database/ent/workflownode"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WorkflowApplicationQuery is the builder for querying WorkflowApplication entities.
type WorkflowApplicationQuery struct {
	config
	ctx                 *QueryContext
	order               []workflowapplication.OrderOption
	inters              []Interceptor
	predicates          []predicate.WorkflowApplication
	withNodes           *WorkflowNodeQuery
	withEdges           *WorkflowEdgeQuery
	withExecutions      *WorkflowExecutionQuery
	modifiers           []func(*sql.Selector)
	withNamedNodes      map[string]*WorkflowNodeQuery
	withNamedEdges      map[string]*WorkflowEdgeQuery
	withNamedExecutions map[string]*WorkflowExecutionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WorkflowApplicationQuery builder.
func (_q *WorkflowApplicationQuery) Where(ps ...predicate.WorkflowApplication) *WorkflowApplicationQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *WorkflowApplicationQuery) Limit(limit int) *WorkflowApplicationQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *WorkflowApplicationQuery) Offset(offset int) *WorkflowApplicationQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *WorkflowApplicationQuery) Unique(unique bool) *WorkflowApplicationQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *WorkflowApplicationQuery) Order(o ...workflowapplication.OrderOption) *WorkflowApplicationQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryNodes chains the current query on the "nodes" edge.
func (_q *WorkflowApplicationQuery) QueryNodes() *WorkflowNodeQuery {
	query := (&WorkflowNodeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workflowapplication.Table, workflowapplication.FieldID, selector),
			sqlgraph.To(workflownode.Table, workflownode.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, workflowapplication.NodesTable, workflowapplication.NodesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEdges chains the current query on the "edges" edge.
func (_q *WorkflowApplicationQuery) QueryEdges() *WorkflowEdgeQuery {
	query := (&WorkflowEdgeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workflowapplication.Table, workflowapplication.FieldID, selector),
			sqlgraph.To(workflowedge.Table, workflowedge.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, workflowapplication.EdgesTable, workflowapplication.EdgesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryExecutions chains the current query on the "executions" edge.
func (_q *WorkflowApplicationQuery) QueryExecutions() *WorkflowExecutionQuery {
	query := (&WorkflowExecutionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workflowapplication.Table, workflowapplication.FieldID, selector),
			sqlgraph.To(workflowexecution.Table, workflowexecution.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, workflowapplication.ExecutionsTable, workflowapplication.ExecutionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first WorkflowApplication entity from the query.
// Returns a *NotFoundError when no WorkflowApplication was found.
func (_q *WorkflowApplicationQuery) First(ctx context.Context) (*WorkflowApplication, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{workflowapplication.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *WorkflowApplicationQuery) FirstX(ctx context.Context) *WorkflowApplication {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WorkflowApplication ID from the query.
// Returns a *NotFoundError when no WorkflowApplication ID was found.
func (_q *WorkflowApplicationQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{workflowapplication.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *WorkflowApplicationQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WorkflowApplication entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WorkflowApplication entity is found.
// Returns a *NotFoundError when no WorkflowApplication entities are found.
func (_q *WorkflowApplicationQuery) Only(ctx context.Context) (*WorkflowApplication, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{workflowapplication.Label}
	default:
		return nil, &NotSingularError{workflowapplication.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *WorkflowApplicationQuery) OnlyX(ctx context.Context) *WorkflowApplication {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WorkflowApplication ID in the query.
// Returns a *NotSingularError when more than one WorkflowApplication ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *WorkflowApplicationQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{workflowapplication.Label}
	default:
		err = &NotSingularError{workflowapplication.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *WorkflowApplicationQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WorkflowApplications.
func (_q *WorkflowApplicationQuery) All(ctx context.Context) ([]*WorkflowApplication, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WorkflowApplication, *WorkflowApplicationQuery]()
	return withInterceptors[[]*WorkflowApplication](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *WorkflowApplicationQuery) AllX(ctx context.Context) []*WorkflowApplication {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WorkflowApplication IDs.
func (_q *WorkflowApplicationQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(workflowapplication.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *WorkflowApplicationQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *WorkflowApplicationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*WorkflowApplicationQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *WorkflowApplicationQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *WorkflowApplicationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *WorkflowApplicationQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WorkflowApplicationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *WorkflowApplicationQuery) Clone() *WorkflowApplicationQuery {
	if _q == nil {
		return nil
	}
	return &WorkflowApplicationQuery{
		config:         _q.config,
		ctx:            _q.ctx.Clone(),
		order:          append([]workflowapplication.OrderOption{}, _q.order...),
		inters:         append([]Interceptor{}, _q.inters...),
		predicates:     append([]predicate.WorkflowApplication{}, _q.predicates...),
		withNodes:      _q.withNodes.Clone(),
		withEdges:      _q.withEdges.Clone(),
		withExecutions: _q.withExecutions.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithNodes tells the query-builder to eager-load the nodes that are connected to
// the "nodes" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkflowApplicationQuery) WithNodes(opts ...func(*WorkflowNodeQuery)) *WorkflowApplicationQuery {
	query := (&WorkflowNodeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withNodes = query
	return _q
}

// WithEdges tells the query-builder to eager-load the nodes that are connected to
// the "edges" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkflowApplicationQuery) WithEdges(opts ...func(*WorkflowEdgeQuery)) *WorkflowApplicationQuery {
	query := (&WorkflowEdgeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEdges = query
	return _q
}

// WithExecutions tells the query-builder to eager-load the nodes that are connected to
// the "executions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkflowApplicationQuery) WithExecutions(opts ...func(*WorkflowExecutionQuery)) *WorkflowApplicationQuery {
	query := (&WorkflowExecutionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withExecutions = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WorkflowApplication.Query().
//		GroupBy(workflowapplication.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *WorkflowApplicationQuery) GroupBy(field string, fields ...string) *WorkflowApplicationGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WorkflowApplicationGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = workflowapplication.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.WorkflowApplication.Query().
//		Select(workflowapplication.FieldCreateTime).
//		Scan(ctx, &v)
func (_q *WorkflowApplicationQuery) Select(fields ...string) *WorkflowApplicationSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &WorkflowApplicationSelect{WorkflowApplicationQuery: _q}
	sbuild.label = workflowapplication.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WorkflowApplicationSelect configured with the given aggregations.
func (_q *WorkflowApplicationQuery) Aggregate(fns ...AggregateFunc) *WorkflowApplicationSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *WorkflowApplicationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !workflowapplication.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *WorkflowApplicationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WorkflowApplication, error) {
	var (
		nodes       = []*WorkflowApplication{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withNodes != nil,
			_q.withEdges != nil,
			_q.withExecutions != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WorkflowApplication).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WorkflowApplication{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withNodes; query != nil {
		if err := _q.loadNodes(ctx, query, nodes,
			func(n *WorkflowApplication) { n.Edges.Nodes = []*WorkflowNode{} },
			func(n *WorkflowApplication, e *WorkflowNode) { n.Edges.Nodes = append(n.Edges.Nodes, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withEdges; query != nil {
		if err := _q.loadEdges(ctx, query, nodes,
			func(n *WorkflowApplication) { n.Edges.Edges = []*WorkflowEdge{} },
			func(n *WorkflowApplication, e *WorkflowEdge) { n.Edges.Edges = append(n.Edges.Edges, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withExecutions; query != nil {
		if err := _q.loadExecutions(ctx, query, nodes,
			func(n *WorkflowApplication) { n.Edges.Executions = []*WorkflowExecution{} },
			func(n *WorkflowApplication, e *WorkflowExecution) { n.Edges.Executions = append(n.Edges.Executions, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedNodes {
		if err := _q.loadNodes(ctx, query, nodes,
			func(n *WorkflowApplication) { n.appendNamedNodes(name) },
			func(n *WorkflowApplication, e *WorkflowNode) { n.appendNamedNodes(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedEdges {
		if err := _q.loadEdges(ctx, query, nodes,
			func(n *WorkflowApplication) { n.appendNamedEdges(name) },
			func(n *WorkflowApplication, e *WorkflowEdge) { n.appendNamedEdges(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedExecutions {
		if err := _q.loadExecutions(ctx, query, nodes,
			func(n *WorkflowApplication) { n.appendNamedExecutions(name) },
			func(n *WorkflowApplication, e *WorkflowExecution) { n.appendNamedExecutions(name, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *WorkflowApplicationQuery) loadNodes(ctx context.Context, query *WorkflowNodeQuery, nodes []*WorkflowApplication, init func(*WorkflowApplication), assign func(*WorkflowApplication, *WorkflowNode)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uint64]*WorkflowApplication)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(workflownode.FieldApplicationID)
	}
	query.Where(predicate.WorkflowNode(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(workflowapplication.NodesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ApplicationID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "application_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *WorkflowApplicationQuery) loadEdges(ctx context.Context, query *WorkflowEdgeQuery, nodes []*WorkflowApplication, init func(*WorkflowApplication), assign func(*WorkflowApplication, *WorkflowEdge)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uint64]*WorkflowApplication)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(workflowedge.FieldApplicationID)
	}
	query.Where(predicate.WorkflowEdge(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(workflowapplication.EdgesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ApplicationID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "application_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *WorkflowApplicationQuery) loadExecutions(ctx context.Context, query *WorkflowExecutionQuery, nodes []*WorkflowApplication, init func(*WorkflowApplication), assign func(*WorkflowApplication, *WorkflowExecution)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uint64]*WorkflowApplication)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(workflowexecution.FieldApplicationID)
	}
	query.Where(predicate.WorkflowExecution(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(workflowapplication.ExecutionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ApplicationID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "application_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *WorkflowApplicationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *WorkflowApplicationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(workflowapplication.Table, workflowapplication.Columns, sqlgraph.NewFieldSpec(workflowapplication.FieldID, field.TypeUint64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, workflowapplication.FieldID)
		for i := range fields {
			if fields[i] != workflowapplication.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *WorkflowApplicationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(workflowapplication.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = workflowapplication.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *WorkflowApplicationQuery) ForUpdate(opts ...sql.LockOption) *WorkflowApplicationQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *WorkflowApplicationQuery) ForShare(opts ...sql.LockOption) *WorkflowApplicationQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// WithNamedNodes tells the query-builder to eager-load the nodes that are connected to the "nodes"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkflowApplicationQuery) WithNamedNodes(name string, opts ...func(*WorkflowNodeQuery)) *WorkflowApplicationQuery {
	query := (&WorkflowNodeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedNodes == nil {
		_q.withNamedNodes = make(map[string]*WorkflowNodeQuery)
	}
	_q.withNamedNodes[name] = query
	return _q
}

// WithNamedEdges tells the query-builder to eager-load the nodes that are connected to the "edges"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkflowApplicationQuery) WithNamedEdges(name string, opts ...func(*WorkflowEdgeQuery)) *WorkflowApplicationQuery {
	query := (&WorkflowEdgeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedEdges == nil {
		_q.withNamedEdges = make(map[string]*WorkflowEdgeQuery)
	}
	_q.withNamedEdges[name] = query
	return _q
}

// WithNamedExecutions tells the query-builder to eager-load the nodes that are connected to the "executions"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkflowApplicationQuery) WithNamedExecutions(name string, opts ...func(*WorkflowExecutionQuery)) *WorkflowApplicationQuery {
	query := (&WorkflowExecutionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedExecutions == nil {
		_q.withNamedExecutions = make(map[string]*WorkflowExecutionQuery)
	}
	_q.withNamedExecutions[name] = query
	return _q
}

// WorkflowApplicationGroupBy is the group-by builder for WorkflowApplication entities.
type WorkflowApplicationGroupBy struct {
	selector
	build *WorkflowApplicationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *WorkflowApplicationGroupBy) Aggregate(fns ...AggregateFunc) *WorkflowApplicationGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *WorkflowApplicationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkflowApplicationQuery, *WorkflowApplicationGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *WorkflowApplicationGroupBy) sqlScan(ctx context.Context, root *WorkflowApplicationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WorkflowApplicationSelect is the builder for selecting fields of WorkflowApplication entities.
type WorkflowApplicationSelect struct {
	*WorkflowApplicationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *WorkflowApplicationSelect) Aggregate(fns ...AggregateFunc) *WorkflowApplicationSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *WorkflowApplicationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkflowApplicationQuery, *WorkflowApplicationSelect](ctx, _s.WorkflowApplicationQuery, _s, _s.inters, v)
}

func (_s *WorkflowApplicationSelect) sqlScan(ctx context.Context, root *WorkflowApplicationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
